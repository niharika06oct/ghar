# Copy to .env (or just export these) before running the backend.
# The key stays server-side and is NEVER sent to the browser.

OPENAI_API_KEY=sk-your-key-here

# Optional overrides:
# PORT=8787
# GHAR_CHAT_MODEL=gpt-4o
# GHAR_IMAGE_MODEL=dall-e-3
